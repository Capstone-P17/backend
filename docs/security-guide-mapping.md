# 공식 보안약점 진단가이드 매핑 테이블

이 문서는 현재 백엔드 정적 분석기가 행정안전부 「소프트웨어 보안약점 진단가이드(2019.6. 개정)」의 어떤 항목을 지원하는지 정리한다.

- 기준 문서: 행정안전부 「소프트웨어 보안약점 진단가이드(2019.6. 개정)」
- 분석 대상: Java 소스코드
- 분석 방식: rule-based static analysis
- 현재 detector 수: 8개
- 구현단계 보안약점 항목 기준 커버리지: 47개 항목 중 10개 항목 매핑
- 주의: 매핑은 “가이드 항목과 detector의 대응 관계”를 의미하며, 해당 항목의 모든 변형을 완전 탐지한다는 뜻은 아니다.

## 분석 엔진 위치

서비스에서 사용하는 정적 분석 엔진은 `src/app/services/static_analysis/` 아래에 있다.

- 분석 진입점: `src/app/services/static_analysis/runner.py`
- AST 파서/공통 유틸리티: `src/app/services/static_analysis/parser.py`
- 취약점별 탐지기: `src/app/services/static_analysis/detectors/`
- 테스트/시연용 Java 샘플: `src/analyzer/test_samples/`

`src/analyzer/test_samples/`는 샘플 코드 위치이며 실제 서비스 분석 엔진 경로가 아니다.

## 현재 지원 Detector

| Detector | CWE | 가이드 분류 | 공식 가이드 항목 | 구현 상태 | 주요 탐지 근거 |
|---|---|---|---|---|---|
| `SQL_INJECTION` | CWE-89 | 입력데이터 검증 및 표현 | SQL 삽입 | 구현 | 사용자 입력 또는 메서드 파라미터가 SQL 문자열 생성 지점을 거쳐 `executeQuery`, `executeUpdate` 등 SQL 실행 API에 도달하는 흐름 |
| `XSS` | CWE-79 | 입력데이터 검증 및 표현 | 크로스사이트 스크립트 | 구현 | 사용자 입력이 HTML 응답 출력 지점에 도달하고, HTML 이스케이프 또는 sanitizer 호출이 확인되지 않는 흐름 |
| `PATH_TRAVERSAL` | CWE-22 | 입력데이터 검증 및 표현 | 경로 조작 및 자원 삽입 | 구현 | 사용자 입력이 `File`, `FileInputStream`, `Paths.get` 등 경로 생성/파일 접근 API에 전달되는 흐름 |
| `COMMAND_INJECTION` | CWE-78 | 입력데이터 검증 및 표현 | 운영체제 명령어 삽입 | 구현 | 사용자 입력이 `Runtime.exec` 또는 `ProcessBuilder` 명령 실행 지점에 전달되는 흐름 |
| `DANGEROUS_FILE_UPLOAD` | CWE-434 | 입력데이터 검증 및 표현 | 위험한 형식 파일 업로드 | 부분 구현 | 업로드 파일이 저장 API에 전달될 때 확장자, Content-Type, 파일 시그니쳐, 크기, 개수, 파일명 재생성, 저장 경로, 실행권한, 다운로드 검증 근거를 확인 |
| `HARDCODED_SECRET` | CWE-798 | 보안기능 | 하드코드된 비밀번호 / 하드코드된 암호화 키 | 부분 구현 | 민감 키워드 변수 또는 비밀값 형식 문자열이 하드코딩된 경우. 민감 호출 사용처가 확인되면 신뢰도와 근거를 강화 |
| `INSECURE_RANDOM` | CWE-338 | 보안기능 | 적절하지 않은 난수값 사용 | 구현 | 토큰, 세션, 키 등 보안 문맥에서 `java.util.Random`이 사용되는 패턴. 비보안 문맥은 제외하거나 낮은 신뢰도로 분류 |
| `WEAK_HASH` | CWE-328 | 보안기능 | 취약한 암호화 알고리즘 사용 / 솔트 없이 일방향 해시함수 사용 | 부분 구현 | `MessageDigest.getInstance("MD5")`, `"SHA-1"`, `"SHA1"` 등 취약 해시 알고리즘 호출과 비밀번호/토큰 문맥의 salt/KDF 없는 일반 해시 사용 |

## 구현단계 가이드 항목 매핑

| No | 공식 가이드 항목 | 가이드 분류 | 페이지 | 연결 Detector | 구현 상태 | 비고 |
|---:|---|---|---|---|---|---|
| 1 | SQL 삽입 | 입력데이터 검증 및 표현 | p.178-191 | `SQL_INJECTION` | 구현 | 문자열 결합, `String.format`, `.concat` 기반 SQL 실행 흐름 탐지 |
| 2 | 경로 조작 및 자원 삽입 | 입력데이터 검증 및 표현 | p.192-201 | `PATH_TRAVERSAL` | 구현 | 사용자 입력이 파일 경로 API에 직접 전달되는 패턴 탐지 |
| 3 | 크로스사이트 스크립트 | 입력데이터 검증 및 표현 | p.202-213 | `XSS` | 구현 | HTML 출력 sink와 sanitizer 미확인 근거 제공 |
| 4 | 운영체제 명령어 삽입 | 입력데이터 검증 및 표현 | p.214-222 | `COMMAND_INJECTION` | 구현 | `Runtime.exec`, `ProcessBuilder` sink 탐지 |
| 5 | 위험한 형식 파일 업로드 | 입력데이터 검증 및 표현 | p.223-229 | `DANGEROUS_FILE_UPLOAD` | 부분 구현 | 업로드 저장 API와 주요 방어 항목 evidence 제공. 실행권한/다운로드 검증은 정적 근거 한계가 있음 |
| 4 | 취약한 암호화 알고리즘 사용 | 보안기능 | p.307-313 | `WEAK_HASH` | 부분 구현 | MD5/SHA-1 계열 `MessageDigest` 탐지 중심. 전체 암호 알고리즘 적절성 판단은 미지원 |
| 7 | 하드코드된 비밀번호 | 보안기능 | p.327-331 | `HARDCODED_SECRET` | 부분 구현 | 사용 여부와 무관하게 민감 키워드 변수의 문자열 리터럴을 탐지. 설정 파일/주석 등 비 Java 선언 외 영역은 미지원 |
| 9 | 적절하지 않은 난수값 사용 | 보안기능 | p.336-341 | `INSECURE_RANDOM` | 구현 | 보안 문맥 변수명과 `new Random()` 사용 근거 제공 |
| 10 | 하드코드된 암호화 키 | 보안기능 | p.342-347 | `HARDCODED_SECRET` | 부분 구현 | 비밀번호와 동일 detector로 처리. 일부 API key/token/private key 형식 패턴을 함께 탐지 |
| 14 | 솔트 없이 일방향 해시함수 사용 | 보안기능 | p.361-364 | `WEAK_HASH` | 부분 구현 | 비밀번호/토큰 문맥에서 `MessageDigest.digest()` 사용 시 salt 또는 KDF 사용 여부를 메서드 범위에서 확인 |

## 설계단계 파일 업로드/다운로드 기준 반영 현황

공식 가이드의 설계단계 `SR1-10 업로드·다운로드 파일검증` 항목은 구현단계의 `위험한 형식 파일 업로드`와 연결된다.

| 설계 기준 | PDF 위치 | 현재 반영 여부 | 현재 evidence 표시 |
|---|---|---|---|
| 업로드 파일 타입 제한 | p.91, p.93, p.514, p.516 | 반영 | 확장자, Content-Type, 파일 시그니쳐/Magic byte 검증 |
| 업로드 파일 크기 제한 | p.91, p.93, p.514 | 반영 | 파일 크기 제한 |
| 업로드 파일 개수 제한 | p.91, p.514 | 반영 | 파일 개수 제한 |
| 업로드 파일 실행권한 제거 | p.91, p.93, p.514, p.517 | 반영 | 실행권한 제거 |
| 외부에서 식별되지 않는 저장 경로 | p.94, p.223, p.514, p.516 | 반영 | 저장 경로 |
| 외부에서 식별되지 않는 저장 파일명 | p.94, p.223, p.514, p.516 | 반영 | 파일명 재생성 |
| 다운로드 요청 파일명 검증 | p.91, p.94, p.517 | 부분 반영 | 업로드 evidence에서 별도 확인 필요로 표시. 독립 다운로드 detector는 미구현 |
| 다운로드 파일 무결성 검사 | p.91, p.517 | 미구현 | 별도 detector 없음 |

## 미구현 가이드 항목

다음 항목은 공식 가이드에는 존재하지만 현재 detector가 없다. 발표 또는 보고서에서는 “현재 지원 범위 밖”으로 명확히 설명해야 한다.

| No | 공식 가이드 항목 | 가이드 분류 | 페이지 |
|---:|---|---|---|
| 6 | 신뢰되지 않는 URL 주소로 자동접속 연결 | 입력데이터 검증 및 표현 | p.230-234 |
| 7 | XQuery 삽입 | 입력데이터 검증 및 표현 | p.235-240 |
| 8 | XPath 삽입 | 입력데이터 검증 및 표현 | p.241-248 |
| 9 | LDAP 삽입 | 입력데이터 검증 및 표현 | p.249-256 |
| 10 | 크로스사이트 요청위조 | 입력데이터 검증 및 표현 | p.257-260 |
| 11 | HTTP 응답분할 | 입력데이터 검증 및 표현 | p.261-266 |
| 12 | 정수형 오버플로우 | 입력데이터 검증 및 표현 | p.267-273 |
| 13 | 보안기능 결정에 사용되는 부적절한 입력값 | 입력데이터 검증 및 표현 | p.274-279 |
| 14 | 메모리 버퍼 오버플로우 | 입력데이터 검증 및 표현 | p.280-285 |
| 15 | 포맷 스트링 삽입 | 입력데이터 검증 및 표현 | p.286-290 |
| 1 | 적절한 인증 없는 중요기능 허용 | 보안기능 | p.291-295 |
| 2 | 부적절한 인가 | 보안기능 | p.296-301 |
| 3 | 중요한 자원에 대한 잘못된 권한 설정 | 보안기능 | p.302-306 |
| 5 | 중요정보 평문저장 | 보안기능 | p.314-318 |
| 6 | 중요정보 평문전송 | 보안기능 | p.319-326 |
| 8 | 충분하지 않은 키 길이 사용 | 보안기능 | p.332-335 |
| 11 | 취약한 비밀번호 허용 | 보안기능 | p.348-352 |
| 12 | 사용자 하드디스크에 저장되는 쿠키를 통한 정보노출 | 보안기능 | p.353-356 |
| 13 | 주석문 안에 포함된 시스템 주요정보 | 보안기능 | p.357-360 |
| 15 | 무결성 검사 없는 코드 다운로드 | 보안기능 | p.365-370 |
| 16 | 반복된 인증시도 제한 기능 부재 | 보안기능 | p.371-376 |
| 1 | 경쟁조건: 검사 시점과 사용 시점(TOCTOU) | 시간 및 상태 | p.377-385 |
| 2 | 종료되지 않는 반복문 또는 재귀함수 | 시간 및 상태 | p.386-389 |
| 1 | 오류 메시지를 통한 정보노출 | 에러처리 | p.390-394 |
| 2 | 오류 상황 대응 부재 | 에러처리 | p.395-398 |
| 3 | 부적절한 예외 처리 | 에러처리 | p.399-402 |
| 1 | Null Pointer 역참조 | 코드오류 | p.403-415 |
| 2 | 부적절한 자원 해제 | 코드오류 | p.416-424 |
| 3 | 해제된 자원 사용 | 코드오류 | p.425-429 |
| 4 | 초기화되지 않은 변수 사용 | 코드오류 | p.430-432 |
| 1 | 잘못된 세션에 의한 데이터 정보노출 | 캡슐화 | p.433-439 |
| 2 | 제거되지 않고 남은 디버그 코드 | 캡슐화 | p.440-444 |
| 3 | 시스템 데이터 정보노출 | 캡슐화 | p.445-448 |
| 4 | Public 메서드로부터 반환된 Private 배열 | 캡슐화 | p.449-454 |
| 5 | Private 배열에 Public 데이터 할당 | 캡슐화 | p.455-459 |
| 1 | DNS lookup에 의존한 보안결정 | API 오용 | p.460-464 |
| 2 | 취약한 API 사용 | API 오용 | p.465-471 |

## 다음 개발 우선순위

| 우선순위 | 작업 | 이유 |
|---:|---|---|
| 1 | 다운로드 파일 검증 detector 추가 | `SR1-10 업로드·다운로드 파일검증`에서 다운로드 요청 파일명 검증과 무결성 검사를 요구하지만 현재 독립 detector가 없다. |
| 2 | `WEAK_HASH` salt/KDF 문맥 보강 | 현재는 MD5/SHA-1 호출 중심이다. 비밀번호 해시에 salt/KDF가 없는 경우를 더 정확히 구분해야 한다. |
| 3 | 오탐/미탐 샘플 확장 | 구현 상태를 설명하려면 각 detector별 vulnerable/safe 샘플 쌍이 필요하다. |
| 4 | 미구현 항목 중 발표 효과가 큰 항목 추가 | HTTP 응답분할, 중요정보 평문저장, 주석문 주요정보, 취약한 API 사용 등이 비교적 구현 대비 설명 효과가 크다. |
